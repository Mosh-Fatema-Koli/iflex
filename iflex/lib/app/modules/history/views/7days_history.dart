import 'package:flutter/material.dart';

class SevendaysHistory extends StatelessWidget {
  const SevendaysHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
