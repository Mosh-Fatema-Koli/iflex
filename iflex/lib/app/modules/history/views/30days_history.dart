import 'package:flutter/material.dart';

class ThirtyDaysHistory extends StatelessWidget {
  const ThirtyDaysHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
