import 'package:flutter/material.dart';

class SixMonthsHistory extends StatelessWidget {
  const SixMonthsHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
